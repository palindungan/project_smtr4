<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sicat">
    <!-- untuk akses internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application xmlns:tools="http://schemas.android.com/tools"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2"
        tools:replace="android:theme">
        <activity android:name=".PanduanActivity"></activity>
        <activity android:name=".activities.TransaksiListDetalItemActivity" />
        <activity android:name=".activities.TransaksiListDetailActivity" />
        <activity android:name=".activities.TransaksiListActivity" />
        <activity android:name=".activities.FormOrderFinishActivity" />
        <activity android:name=".activities.FormOrderActivity" />
        <activity android:name=".activities.DaftarBonusActivity" />
        <activity android:name=".activities.CartDetailActivity" />
        <activity android:name=".activities.PaketListActivity" />
        <activity android:name=".activities.CartActivity" />
        <activity
            android:name=".activities.detail.DetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.category.CategoryActivity"
            android:label="Category"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".activities.daftar_menu.MenuActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".activities.AccountActivity" />
        <activity
            android:name=".activities.HomeActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.FaceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>