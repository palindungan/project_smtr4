<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme2" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus" tools:targetApi="kitkat">true</item>
    </style>

    <style name="AppBarLayoutTheme" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">@color/colorWhite</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Custom Style  -->
    <style name="Base.Theme.RAKHMAT_FADILAH" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimaryOrange</item>

        <!-- Warna Gelap Untuk Status Bar -->
        <item name="colorPrimaryDark">@color/colorDarkPrimaryOrange</item>

        <!-- Warna untuk Radio Button / FloatActionButton -->
        <item name="colorAccent">@color/colorAccentOrange</item>

        <!-- Warna Untuk Judul Text  -->
        <item name="android:textColorPrimary">@color/colorPrimaryText</item>

        <!-- Warna untuk menu overflow icon -->
        <item name="android:textColorSecondary">@color/colorSecondaryText</item>
    </style>
    <!-- Base Application Theme -->
    <style name="AppBaseTheme1" parent="Base.Theme.RAKHMAT_FADILAH"></style>

    <!-- Application Theme -->
    <style name="AppTheme1" parent="AppBaseTheme1"></style>

</resources>
